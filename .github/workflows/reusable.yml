name: Reusable deploy
on: 
    workflow_call:  
jobs:
    deploy:
        runs-on: ubuntu-latest
        steps:
            - name: Deploy from reusable workflow
              run: echo "Deploying & uploading"
